name: section12_cinemapedia
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  card_swiper: ^3.0.1
  go_router: ^17.3.0
  dio: ^5.11.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.3
  flutter_dotenv: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
